[project]
name = "medical-claims-detector"
version = "0.1.0"
dependencies = [
    "requests>=2.31.0",
    "pandas>=2.0.0",
    "xgboost>=2.0.3",
]

[project.optional-dependencies]
dev = ["pytest", "black", "ruff"]

[tool.ruff]
line-length = 100